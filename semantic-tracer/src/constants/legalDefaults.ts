import { EntityType } from '../types/entities';

export const NODE_DIMENSIONS = { width: 160, height: 60 };

export const DEFAULTS = {
  entityType: EntityType.Entity,
  spacing: { x: 48, y: 48 },
};
